extends layout

block content
  h1 Posts

  if postList
    ul
      each post in postList
        li: a(href='/posts/' + post.slug)= post.title

  h2 New Post

  form(method='post')
    label Title
      input(type='text', name='title')
    br
    label Body
      textarea(name='body')
    br
    button(type='submit') Submit